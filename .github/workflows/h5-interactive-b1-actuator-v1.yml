name: H5 Interactive B1 Actuator V1

# Temporary deterministic Gate-4 actuator for CONTROL-199 H5.
# GitHub owns claim/result/finalization; this workflow performs no semantic inference.
# It lives only on the isolated actuator branch and does not change main or PR #69.

on:
  push:
    branches:
      - control/h5-interactive-b1-actuator-v1
    paths:
      - .control/h5-interactive-command.json
      - .github/workflows/h5-interactive-b1-actuator-v1.yml

permissions:
  contents: read
  statuses: write

concurrency:
  group: control-h5-interactive-b1-actuator-v1
  cancel-in-progress: false

env:
  CONTROL_PLANE_REPOSITORY: market-predictions/control-plane
  CONTROL_RUNTIME_REF: control-runtime-state
  CONTROL_CODE_REF: runtime/public-b-v2-code-r2
  CONTROL_TASK_ID: CONTROL-199-PR69-INDEPENDENT-B1-ASSURE-H5
  CONTROL_CANDIDATE_SHA: 7674b04763d70b0d10635beafe539bc3097a56fb
  CONTROL_ISSUE_NUMBER: '199'

jobs:
  actuator:
    if: github.repository == 'market-predictions/control-engine' && github.ref == 'refs/heads/control/h5-interactive-b1-actuator-v1'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout isolated actuator branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: false
          fetch-depth: 1

      - name: Validate public command envelope
        id: command
        shell: bash
        run: |
          set -euo pipefail
          file=.control/h5-interactive-command.json
          jq -e '
            .version == "1.0" and
            (.action == "hold" or .action == "claim" or .action == "complete") and
            .task_id == env.CONTROL_TASK_ID and
            .candidate_sha == env.CONTROL_CANDIDATE_SHA
          ' "$file" >/dev/null
          action="$(jq -r '.action' "$file")"
          printf 'action=%s\n' "$action" >> "$GITHUB_OUTPUT"
          if [ "$action" = complete ]; then
            jq -e '
              (.active_run_id | type == "string") and
              (.active_run_id | test("^run-[0-9a-f-]+$")) and
              (.review_comment_id | type == "number") and
              .review_comment_id > 0
            ' "$file" >/dev/null
          fi

      - name: Hold without private mutation
        if: steps.command.outputs.action == 'hold'
        run: echo 'H5_ACTUATOR=HOLD'

      - name: Preflight GitHub App identity and installation
        id: app-preflight
        if: steps.command.outputs.action != 'hold'
        shell: bash
        env:
          CONTROL_GITHUB_APP_ID: ${{ vars.CONTROL_GITHUB_APP_ID }}
          CONTROL_GITHUB_APP_PRIVATE_KEY: ${{ secrets.CONTROL_GITHUB_APP_PRIVATE_KEY }}
        run: bash scripts/github_app_preflight.sh

      - name: Create short-lived Control GitHub App token
        id: app-token
        if: steps.command.outputs.action != 'hold'
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1
        with:
          app-id: ${{ vars.CONTROL_GITHUB_APP_ID }}
          private-key: ${{ secrets.CONTROL_GITHUB_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          permission-contents: 'write'
          permission-issues: 'read'

      - name: Setup Python
        if: steps.command.outputs.action != 'hold'
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'

      - name: Fetch pinned private runtime code
        if: steps.command.outputs.action != 'hold'
        shell: bash
        env:
          CONTROL_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          code="$RUNNER_TEMP/control-code"
          url="https://x-access-token:${CONTROL_TOKEN}@github.com/${CONTROL_PLANE_REPOSITORY}.git"
          git clone --quiet --single-branch --branch "$CONTROL_CODE_REF" "$url" "$code"
          printf 'CONTROL_CODE_DIR=%s\n' "$code" >> "$GITHUB_ENV"

      - name: Claim exact preferred H5 B1 task
        if: steps.command.outputs.action == 'claim'
        shell: bash
        env:
          CONTROL_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          state="$RUNNER_TEMP/control-state"
          selection="$RUNNER_TEMP/h5-selection.json"
          url="https://x-access-token:${CONTROL_TOKEN}@github.com/${CONTROL_PLANE_REPOSITORY}.git"
          git clone --quiet --single-branch --branch "$CONTROL_RUNTIME_REF" "$url" "$state"

          python control_engine/scheduled_worker_b.py select-b1 \
            --code-dir "$CONTROL_CODE_DIR" \
            --queue "$state/control/DISPATCH_QUEUE.json" \
            --output "$selection" >/dev/null

          python - "$selection" <<'PY'
          import json, os, sys
          value = json.load(open(sys.argv[1], encoding='utf-8'))
          if value.get('selected') is not True:
              raise SystemExit('H5 is not currently selectable')
          if value.get('task_id') != os.environ['CONTROL_TASK_ID']:
              raise SystemExit('H5 is not the deterministic preferred B1 task')
          if value.get('candidate_sha') != os.environ['CONTROL_CANDIDATE_SHA']:
              raise SystemExit('H5 candidate binding mismatch')
          PY

          GH_TOKEN="$CONTROL_TOKEN" PYTHONPATH="$CONTROL_CODE_DIR" \
            python "$CONTROL_CODE_DIR/tools/control_connected_worker_runtime_v1.py" claim \
              --runtime-root "$state" \
              --runtime-ref "$CONTROL_RUNTIME_REF" \
              --task-id "$CONTROL_TASK_ID" \
              --worker-instance B1 \
              --backend github-actions/interactive-b1-actuator-v1 \
              --lease-seconds 7200 \
              > "$RUNNER_TEMP/h5-claim-private.json"

          echo 'H5_ACTUATOR=CLAIM_PERSISTED'

      - name: Read and validate independent semantic verdict
        if: steps.command.outputs.action == 'complete'
        id: verdict
        shell: bash
        env:
          CONTROL_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          command=.control/h5-interactive-command.json
          comment_id="$(jq -r '.review_comment_id' "$command")"
          active_run_id="$(jq -r '.active_run_id' "$command")"
          result="$RUNNER_TEMP/h5-reviewer-result.json"

          GH_TOKEN="$CONTROL_TOKEN" gh api \
            "repos/${CONTROL_PLANE_REPOSITORY}/issues/comments/${comment_id}" \
            --jq '.body' > "$result"

          jq -e --arg task "$CONTROL_TASK_ID" --arg candidate "$CONTROL_CANDIDATE_SHA" --arg run "$active_run_id" '
            .version == "1.0" and
            .task_id == $task and
            .run_id == $run and
            .role == "governance_release_assurance" and
            (.outcome == "PASS" or .outcome == "FAIL" or .outcome == "INDETERMINATE") and
            .candidate_sha == $candidate and
            (.summary | type == "string") and
            (.findings | type == "array") and all(.findings[]; type == "string") and
            (.evidence | type == "array") and all(.evidence[]; type == "string") and
            (keys | sort) == (["candidate_sha","evidence","findings","outcome","role","run_id","summary","task_id","version"] | sort)
          ' "$result" >/dev/null

          printf 'ACTIVE_RUN_ID=%s\n' "$active_run_id" >> "$GITHUB_ENV"
          printf 'REVIEWER_RESULT=%s\n' "$result" >> "$GITHUB_ENV"

      - name: Complete same exact H5 claim through connected runtime
        if: steps.command.outputs.action == 'complete'
        shell: bash
        env:
          CONTROL_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          set -euo pipefail
          url="https://x-access-token:${CONTROL_TOKEN}@github.com/${CONTROL_PLANE_REPOSITORY}.git"
          success=false
          for attempt in 1 2 3; do
            state="$RUNNER_TEMP/control-state-complete-${attempt}"
            rm -rf "$state"
            git clone --quiet --single-branch --branch "$CONTROL_RUNTIME_REF" "$url" "$state"
            set +e
            GH_TOKEN="$CONTROL_TOKEN" PYTHONPATH="$CONTROL_CODE_DIR" \
              python "$CONTROL_CODE_DIR/tools/control_connected_worker_runtime_v1.py" complete \
                --runtime-root "$state" \
                --runtime-ref "$CONTROL_RUNTIME_REF" \
                --github-repository "$CONTROL_PLANE_REPOSITORY" \
                --task-id "$CONTROL_TASK_ID" \
                --worker-instance B1 \
                --active-run-id "$ACTIVE_RUN_ID" \
                --result "$REVIEWER_RESULT" \
                > "$RUNNER_TEMP/h5-complete-${attempt}.log" 2>&1
            rc=$?
            set -e
            if [ "$rc" = 0 ]; then
              success=true
              break
            fi
            if grep -q 'CONTROL_RUNTIME_CAS_CONFLICT' "$RUNNER_TEMP/h5-complete-${attempt}.log"; then
              continue
            fi
            echo 'H5_ACTUATOR=COMPLETE_FAILED_CLOSED'
            exit 2
          done
          [ "$success" = true ] || { echo 'H5_ACTUATOR=COMPLETE_CAS_EXHAUSTED'; exit 2; }
          echo 'H5_ACTUATOR=COMPLETE_PERSISTED'

      - name: Publish bounded actuator liveness
        if: always()
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          ACTION: ${{ steps.command.outputs.action }}
          JOB_STATUS: ${{ job.status }}
        run: |
          set -euo pipefail
          action="${ACTION:-unknown}"
          job_status="${JOB_STATUS:-failure}"
          state=failure
          [ "$job_status" = success ] && state=success
          description="H5_${action}_${job_status}"
          gh api -X POST "repos/${GITHUB_REPOSITORY}/statuses/${GITHUB_SHA}" \
            -f state="$state" \
            -f context="control/h5-interactive-actuator" \
            -f description="$description" >/dev/null
